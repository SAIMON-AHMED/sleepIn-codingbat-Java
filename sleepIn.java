public boolean sleepIn(boolean weekday, boolean vacation) {
  return (weekday == false || vacation == true);//when weekday false or vacation true,
                                                // we will return true.
}
